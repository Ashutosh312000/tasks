let arr=[1,2,3,6,3,7]

let max=arr[0];
let max2=arr[0];

for(let i=0;i<arr.length;i++){
    if(arr[i]>max){
        max2=max;
        max=arr[i];
    }
    else{
        if(arr[i]>max2){
            max2=arr[i]
        }
    }
}
console.log(max2);
